name: live-departures-backend
replicaCount: 1

image:
  repository: ghcr.io/OWNER/live-departures-backend
  pullPolicy: IfNotPresent
  tag: ""

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi